import React from 'react';
import styles from './styles'

export const Header = () => (
    <div style={styles.header}>
        <div style={styles.headerText}>
            Sudoku helper
        </div>
    </div>
);